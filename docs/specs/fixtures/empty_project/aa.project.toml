schema_version = 1
name = "empty_project"
version = "0.1.0"

[engine]
config_root = "config"
assets_root = "assets"

[plugins]
runtime = []

[features]
enabled = []

[build]
default_binary = "empty_project"
